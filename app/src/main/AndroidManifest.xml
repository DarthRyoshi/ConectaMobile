<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
    <activity
        android:name=".LoginActivity"
        android:exported="true"
        android:theme="@style/Theme.ConectaMobile">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!-- MainActivity: Verificar estado de autenticación y redirigir -->
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:theme="@style/Theme.ConectaMobile" />

    <!-- RegisterActivity: Actividad de registro -->
    <activity
        android:name=".RegisterActivity"
        android:exported="true"
        android:theme="@style/Theme.ConectaMobile" />

    <!-- ContactsActivity: Lista de contactos -->
    <activity
        android:name=".ContactsActivity"
        android:exported="true"
        android:theme="@style/Theme.ConectaMobile" />

    <!-- ChatActivity: Actividad de chat -->
    <activity
        android:name=".ChatActivity"
        android:exported="true"
        android:theme="@style/Theme.ConectaMobile" />

</application>

    </manifest>